name: samtools
url : https://github.com/samtools/samtools/archive/1.1.tar.gz
version : 1.1

dependencies :
    - htslib

build : 
    - make HTSDIR={{ packages.htslib.source_dir }}

install :
    - mkdir -p $installdir/bin
    - cp samtools $installdir/bin